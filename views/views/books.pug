
h2.center Books Table

    p.center Click here to #[a(href="/books1/bookadd") add a book]!

    table.table
        thead
            tr
                th Id
                th Title
                th Edit
                th Delete
        tbody
            each book in books
                tr
                    td
                        a(href=`/books/${book.id}`)= book.id
                    td= book.title
                    td
                        a(href=`/books/${book.id}/edit`) Edit
                    td
                        form(method="POST", action=`/books/${book.id}/?_method=DELETE`)
                            button(type="submit", onclick="return confirm('Are you sure you want to delete this record?')") Delete
                    td
                        a(href=`/books/${book.id}/issue`) Issue
                    td
                        a(href=`/books/${book.id}/return`) Return

    // Pagination links
    if (totalPages > 1)
        ul.pagination
            if currentPage > 1

                    a(href=`/books?page=${currentPage - 1}`) Previous
                    |   <!-- Add a space between links -->
            each page in Array.from({ length: totalPages }, (_, i) => i + 1)
                    a(href=`/books?page=${page}`)= page
                    |   <!-- Add a space between links -->
            if currentPage < totalPages
                    a(href=`/books?page=${currentPage + 1}`) Next
                    |   <!-- Add a space between links -->

    p.center Click #[a(href="/") here] to go back to Homepage!