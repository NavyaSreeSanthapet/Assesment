h2.center Authors
p.center Click here to add new #[a(href='/authors/addauthor')  Author]!

table.table
    thead
        tr
            th id
            th author
    tbody
        each author in authors
            tr
                td
                    td= author.id
                td
                    a(href=`/authors/${author.authorname}/books`)= author.authorname
                td
                    a(href=`/authors/${author.id}/edit`) Edit
                td
                    form(method="POST", action=`/authors/${author.id}/?_method=DELETE`)
                        button(type="submit", onclick="return confirm('Are you sure you want to delete this record?')") Delete


p click  #[a(href="/") here] to go back to Homepage!
